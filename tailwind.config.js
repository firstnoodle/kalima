module.exports = {
    purge: [],
    darkMode: 'class', // or 'media' or 'class'
    theme: {
        extend: {},
        fontFamily: {
            sans: ['K24Kurdish', 'Helvetica', 'Arial', 'sans-serif'],
        }
    },
    variants: {
        extend: {},
    },
    plugins: [],
}
